# 子午流注养生打卡小程序 - 全局视觉规范

> **版本**：V19 琉璃高饱和混血版
> **适用范围**：全小程序视觉设计
> **更新日期**：2026-04-16

---

## 一、设计原则

### 1.1 设计理念

**「玉露丹青」视觉体系**

- **清透感**：琉璃质感、高透明度、呼吸感
- **国潮色**：传统五行色彩现代化演绎
- **高对比**：文字与背景强对比，确保可读性
- **一致性**：全局变量统一管控，组件化复用

### 1.2 色彩哲学

| 五行 | 色彩 | 属性 | 应用场景 |
|------|------|------|----------|
| 木 | 翠微绿 | 生发、运动 | 运动类习惯 |
| 火 | 妃子红 | 温热、理疗 | 理疗类习惯 |
| 土 | 栀子黄 | 中和、起居 | 起居类习惯 |
| 水 | 晴山蓝 | 润下、其他 | 其他类习惯 |
| 金/特殊 | 紫丁香 | 柔韧、特殊 | 特殊类习惯 |

---

## 二、全局色彩体系

### 2.1 基础色板

#### 2.1.1 背景色

| 变量名 | 色值 | 用途 | 使用场景 |
|--------|------|------|----------|
| `--bg-body` | `#F4F6F8` | 页面底色 | 全局页面背景 |
| `--bg-card` | `#FFFFFF` | 卡片背景 | 信息卡片、弹窗 |
| `--bg-primary` | `#F4F6F8` | 兼容变量 | 映射到 `--bg-body` |

#### 2.1.2 文字色

| 变量名 | 色值 | 对比度 | 用途 | 使用场景 |
|--------|------|--------|------|----------|
| `--ink-title` | `#141618` | 16.8:1 | 标题黑 | 页面标题、重要信息 |
| `--ink-main` | `#50565C` | 7.2:1 | 正文灰 | 正文内容、描述文字 |
| `--text-sub` | `#8B959E` | 4.1:1 | 辅助灰 | 辅助信息、时间戳 |
| `--text-primary` | `#141618` | 兼容 | 映射到 `--ink-title` |
| `--text-secondary` | `#50565C` | 兼容 | 映射到 `--ink-main` |
| `--text-tertiary` | `#8B959E` | 兼容 | 映射到 `--text-sub` |

### 2.2 主题色板

#### 2.2.1 主色与浅色

| 变量名 | 色值 | RGB | 用途 |
|--------|------|-----|------|
| `--c-primary` | `#38A385` | 56, 163, 133 | 主色/翠微绿/木 |
| `--c-primary-light` | `#E2F2ED` | 226, 242, 237 | 主色背景 |
| `--c-coral` | `#F0655B` | 240, 101, 91 | 妃子红/火 |
| `--c-coral-light` | `#FDE8E7` | 253, 232, 231 | 火色背景 |
| `--c-amber` | `#F2A93B` | 242, 169, 59 | 栀子黄/土 |
| `--c-amber-light` | `#FEF3E1` | 254, 243, 225 | 土色背景 |
| `--c-lake` | `#5A8CB6` | 90, 140, 182 | 晴山蓝/水 |
| `--c-lake-light` | `#E7F0F6` | 231, 240, 246 | 水色背景 |
| `--c-purple` | `#9D7BB0` | 157, 123, 176 | 紫丁香/特殊 |
| `--c-purple-light` | `#F3EBF6` | 243, 235, 246 | 特殊背景 |

#### 2.2.2 图标专用色

| 变量名 | 色值 | 五行属性 | 习惯分类 |
|--------|------|----------|----------|
| `--c-green` | `#38A385` | 木 | 运动类 |
| `--c-red` | `#F0655B` | 火 | 理疗类 |
| `--c-yellow` | `#F2A93B` | 土 | 起居类 |
| `--c-blue` | `#5A8CB6` | 水 | 其他类 |
| `--c-purple` | `#9D7BB0` | 金/特殊 | 特殊类 |

#### 2.2.3 图标背景色

| 变量名 | 色值 | 对应主色 | 用途 |
|--------|------|----------|------|
| `--bg-green` | `#E2F2ED` | 翠微绿 | 运动类图标底座 |
| `--bg-red` | `#FDE8E7` | 妃子红 | 理疗类图标底座 |
| `--bg-yellow` | `#FEF3E1` | 栀子黄 | 起居类图标底座 |
| `--bg-blue` | `#E7F0F6` | 晴山蓝 | 其他类图标底座 |
| `--bg-purple` | `#F3EBF6` | 紫丁香 | 特殊类图标底座 |

### 2.3 主题色映射逻辑

#### 2.3.1 动态主题类

```css
/* 主题色分配与专属发光阴影 */
.theme-green, .t-green {
    --theme-color: var(--c-primary);
    --theme-bg: var(--c-primary-light);
    --icon-shadow: 0 8px 20px rgba(56, 163, 133, 0.08);
}

.theme-red, .t-red {
    --theme-color: var(--c-coral);
    --theme-bg: var(--c-coral-light);
    --icon-shadow: 0 8px 20px rgba(240, 101, 91, 0.08);
}

.theme-yellow, .t-yellow {
    --theme-color: var(--c-amber);
    --theme-bg: var(--c-amber-light);
    --icon-shadow: 0 8px 20px rgba(242, 169, 59, 0.08);
}

.theme-blue, .t-blue {
    --theme-color: var(--c-lake);
    --theme-bg: var(--c-lake-light);
    --icon-shadow: 0 8px 20px rgba(90, 140, 182, 0.08);
}

.theme-purple, .t-purple {
    --theme-color: var(--c-purple);
    --theme-bg: var(--c-purple-light);
    --icon-shadow: 0 8px 20px rgba(157, 123, 176, 0.08);
}
```

#### 2.3.2 使用方式

```html
<!-- 在 WXML 中动态赋予主题类 -->
<view class="habit-item t-green">
  <view class="icon-wrapper">
    <image class="habit-icon" src="{{iconUrl}}" />
  </view>
  <text class="habit-name">金刚功</text>
</view>
```

```css
/* 使用主题变量 */
.icon-wrapper {
  background-color: var(--theme-bg);
  box-shadow: var(--icon-shadow);
}

.habit-name {
  color: var(--theme-color);
}
```

---

## 三、边框与光影

### 3.1 边框规范

| 变量名 | 值 | 用途 | 使用场景 |
|--------|-----|------|----------|
| `--border-light` | `rgba(0,0,0,0.04)` | 浅色边框 | 分割线、细边框 |

#### 3.1.1 边框使用规范

```css
/* 细分割线 */
.divider {
  border-top: 1px solid var(--border-light);
}

/* 卡片边框 */
.card-border {
  border: 1px solid var(--border-light);
  border-radius: var(--radius-card);
}
```

### 3.2 阴影体系

#### 3.2.1 阴影变量

| 变量名 | 值 | 用途 | 特性 |
|--------|-----|------|------|
| `--shadow-card` | `0 4px 16px rgba(0,0,0,0.02)` | 卡片阴影 | 微弱投影 |
| `--shadow-btn` | `0 8px 20px rgba(56,163,133,0.25)` | 按钮主色发光 | 主色光晕 |

#### 3.2.2 阴影使用示例

```css
/* 基础卡片 */
.card {
  box-shadow: var(--shadow-card);
}

/* 主按钮 */
.btn-primary {
  background-color: var(--c-primary);
  box-shadow: var(--shadow-btn);
}
```

---

## 四、排版与字体

### 4.1 字体栈

```css
/* 苹果设备优先使用苹方，安卓使用系统黑体 */
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", Arial, sans-serif;
```

### 4.2 字号阶梯

| 变量名 | 字号 | 行高 | 字重 | 用途 | 使用场景 |
|--------|------|------|------|------|----------|
| `--text-giant` | 48px | 1.1 | 700 | 巨型数字 | 时辰显示、大数字 |
| `--text-h1` | 22px | 1.3 | 600 | 页面主标题 | 页面大标题 |
| `--text-h2` | 17px | 1.4 | 600 | 导航/卡片标题 | 导航栏、卡片标题 |
| `--text-body` | 15px | 1.6 | 400/500 | 正文信息 | 正文、描述 |
| `--text-caption` | 12px | 1.5 | 400 | 辅助标签 | 标签、时间戳 |

### 4.3 字重规范

| 类名 | 字重 | 用途 | 使用场景 |
|------|------|------|----------|
| `.text-regular` | 400 | 常规 | 正文、描述 |
| `.text-medium` | 500 | 中等 | 次要标题、强调文字 |
| `.text-bold` | 600 | 粗体 | 大多数标题、关键信息 |
| `.text-heavy` | 700 | 特粗 | 超大字号时辰、数据展示 |

### 4.4 排版工具类

```css
/* 文字对齐 */
.text-left { text-align: left; }
.text-center { text-align: center; }
.text-right { text-align: right; }

/* 文字截断 */
.truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.truncate-2 {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
```

---

## 五、空间网格系统

### 5.1 间距变量

| 变量名 | 值 | 用途 | 使用场景 |
|--------|-----|------|----------|
| `--page-padding` | 24px | 页面左右安全边距 | 全局页面内边距 |
| `--gap-list` | 14px | 列表类间距 | 列表项之间 |
| `--gap-module` | 24px | 模块间距 | 大模块之间 |

### 5.2 安全区适配

| 变量名 | 值 | 用途 |
|--------|-----|------|
| `--safe-bottom` | `env(safe-area-inset-bottom, 20px)` | 底部安全区 |

#### 5.2.1 安全区使用

```css
/* TabBar 安全区适配 */
.tab-bar {
  padding-bottom: var(--safe-bottom);
}

/* 页面底部内容 */
.page-content {
  padding-bottom: calc(80px + var(--safe-bottom));
}
```

### 5.3 布局工具类

```css
/* Flex 布局 */
.flex { display: flex; }
.flex-col { display: flex; flex-direction: column; }
.flex-center { 
  display: flex; 
  align-items: center; 
  justify-content: center; 
}
.flex-between { 
  display: flex; 
  align-items: center; 
  justify-content: space-between; 
}
.flex-1 { flex: 1; }
```

---

## 六、圆角包裹规范

### 6.1 圆角变量

| 变量名 | 值 | 用途 | 使用场景 |
|--------|-----|------|----------|
| `--radius-icon` | 50% | 圆形 | 图标、头像 |
| `--radius-card` | 16px | 标准卡片 | 信息卡片、按钮 |
| `--radius-container` | 20px | 较大容器 | 大卡片、模块 |

### 6.2 圆角使用规范

#### 6.2.1 圆形元素 (50%)

```css
/* 习惯图标 */
.habit-icon {
  width: 48px;
  height: 48px;
  border-radius: 50%;
}

/* 用户头像 */
.avatar {
  width: 64px;
  height: 64px;
  border-radius: 50%;
}
```

#### 6.2.2 标准圆角 (16px)

```css
/* 信息卡片 */
.info-card {
  border-radius: var(--radius-card);
  padding: 16px;
}

/* 主按钮 */
.btn-primary {
  border-radius: var(--radius-card);
  height: 48px;
}
```

#### 6.2.3 大圆角 (20px)

```css
/* 大容器 */
.container-card {
  border-radius: var(--radius-container);
  padding: 24px;
}
```

---

## 七、组件规范

### 7.1 按钮规范

#### 7.1.1 主按钮

```css
.btn-primary {
  height: 48px;
  padding: 0 24px;
  background-color: var(--c-primary);
  color: #FFFFFF;
  font-size: 16px;
  font-weight: 600;
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-btn);
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-primary:active {
  transform: scale(0.96);
  box-shadow: none;
}
```

#### 7.1.2 次要按钮

```css
.btn-secondary {
  height: 48px;
  padding: 0 24px;
  background-color: var(--c-primary-light);
  color: var(--c-primary);
  font-size: 16px;
  font-weight: 600;
  border-radius: var(--radius-card);
}
```

### 7.2 卡片规范

#### 7.2.1 标准卡片

```css
.card {
  background-color: var(--bg-card);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  padding: 16px;
}
```

### 7.3 图标规范

#### 7.3.1 习惯图标

```css
.habit-icon-wrapper {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background-color: var(--theme-bg);
  display: flex;
  align-items: center;
  justify-content: center;
}

.habit-icon {
  width: 24px;
  height: 24px;
}
```

### 7.4 弹窗规范（新增）

#### 7.4.1 底部弹窗（Picker Modal）

**结构组成：**
- 黑色蒙层（`picker-modal-mask`）：半透明黑色背景，点击关闭弹窗
- 弹窗容器（`picker-modal`）：白色背景，顶部圆角40rpx
- 头部（`picker-header-simple`）：标题 + 关闭按钮
- 内容区：根据类型分为 picker-view 滚动选择或列表选择
- 底部按钮区（`picker-footer`）：确定按钮

**样式规范：**

```css
/* 弹窗蒙层 */
.picker-modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 10000;
}

/* 弹窗容器 */
.picker-modal {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #fff;
  border-radius: 40rpx 40rpx 0 0;
  max-height: 85vh;
  z-index: 10001;
  display: flex;
  flex-direction: column;
}

/* 弹窗头部 */
.picker-header-simple {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 40rpx 24rpx;
}

.picker-simple-title {
  font-size: 30rpx;
  color: var(--text-primary);
  font-weight: 600;
}

.picker-close {
  font-size: 28rpx;
  color: var(--text-tertiary);
  width: 62rpx;
  height: 62rpx;
  border-radius: 50%;
  background-color: rgba(0, 0, 0, 0.06);
  display: flex;
  align-items: center;
  justify-content: center;
}
```

#### 7.4.2 Picker-View 滚动选择器

**适用场景：** 时长选择、间隔天数选择、日期选择（单选）

**样式规范：**

```css
/* 选择器容器 */
.date-picker-container {
  padding: 0;
  background-color: #fff;
  position: relative;
  overflow: hidden;
}

/* 上下渐变遮罩 */
.date-picker-container::before,
.date-picker-container::after {
  content: '';
  position: absolute;
  left: 0;
  right: 0;
  height: 80rpx;
  pointer-events: none;
  z-index: 10;
}

.date-picker-container::before {
  top: 0;
  background: linear-gradient(to bottom, #fff 0%, rgba(255,255,255,0.98) 60%, transparent 100%);
}

.date-picker-container::after {
  bottom: 0;
  background: linear-gradient(to top, #fff 0%, rgba(255,255,255,0.98) 60%, transparent 100%);
}

/* picker-view 主体 */
.date-picker-view {
  height: 480rpx;
  width: 100%;
  mask-image: linear-gradient(to bottom, transparent 0%, #000 15%, #000 85%, transparent 100%);
}

/* 选项样式 */
.date-picker-item {
  line-height: 120rpx;
  height: 120rpx;
  text-align: center;
  font-size: 32rpx;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 选中态 */
.date-picker-item.selected {
  font-size: 36rpx;
}
```

**交互规则：**
- 默认选中当前值（时长默认20分钟，日期默认今天）
- 滚动时上下选项渐隐
- 选中项文字放大2号（36rpx）
- 点击确定按钮确认选择

#### 7.4.3 列表选择器（复选）

**适用场景：** 星期选择（周一到周日可多选）

**样式规范：**

```css
/* 列表容器 */
.weekday-list {
  padding: 20rpx 40rpx;
  overflow-y: visible;
}

/* 选项样式 */
.weekday-option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 0;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.05);
  font-size: 32rpx;
  color: var(--text-tertiary);  /* 未选中态：浅灰色 */
}

.weekday-option:last-child {
  border-bottom: none;
}

/* 选中态 */
.weekday-option.checked {
  color: var(--c-primary);  /* 主题绿色 */
}

.weekday-check {
  font-size: 32rpx;
  color: var(--c-primary);
  font-weight: bold;
}
```

**交互规则：**
- 点击选项切换选中状态
- 可多选（适用于星期选择）
- 选中项显示对勾标记
- 未选中态文字为浅灰色（`--text-tertiary`）
- 选中态文字为主题绿色（`--c-primary`）

#### 7.4.4 确定按钮

```css
.picker-footer {
  padding: 24rpx 40rpx 48rpx;
  background-color: #fff;
}

.btn-confirm {
  width: 100%;
  padding: 28rpx 0;
  background-color: var(--c-primary);
  color: #fff;
  font-size: 32rpx;
  border-radius: 999rpx;
  border: none;
  font-weight: 600;
  box-shadow: 0 4rpx 20rpx rgba(56, 163, 133, 0.4);
}
```

---

## 八、变量汇总表

### 8.1 颜色变量

| 变量名 | 色值 | 说明 |
|--------|------|------|
| `--bg-body` | `#F4F6F8` | 页面背景 |
| `--bg-card` | `#FFFFFF` | 卡片背景 |
| `--bg-primary` | `#F4F6F8` | 页面背景（兼容） |
| `--ink-title` | `#141618` | 标题文字 |
| `--ink-main` | `#50565C` | 正文文字 |
| `--text-sub` | `#8B959E` | 辅助文字 |
| `--text-primary` | `#141618` | 标题文字（兼容） |
| `--text-secondary` | `#50565C` | 正文文字（兼容） |
| `--text-tertiary` | `#8B959E` | 辅助文字（兼容） |
| `--c-primary` | `#38A385` | 主色/绿 |
| `--c-primary-light` | `#E2F2ED` | 主色背景 |
| `--c-coral` | `#F0655B` | 红色 |
| `--c-coral-light` | `#FDE8E7` | 红色背景 |
| `--c-amber` | `#F2A93B` | 黄色 |
| `--c-amber-light` | `#FEF3E1` | 黄色背景 |
| `--c-lake` | `#5A8CB6` | 蓝色 |
| `--c-lake-light` | `#E7F0F6` | 蓝色背景 |
| `--c-purple` | `#9D7BB0` | 紫色 |
| `--c-purple-light` | `#F3EBF6` | 紫色背景 |
| `--c-green` | `#38A385` | 绿色（图标） |
| `--c-red` | `#F0655B` | 红色（图标） |
| `--c-yellow` | `#F2A93B` | 黄色（图标） |
| `--c-blue` | `#5A8CB6` | 蓝色（图标） |
| `--bg-green` | `#E2F2ED` | 绿色背景（图标） |
| `--bg-red` | `#FDE8E7` | 红色背景（图标） |
| `--bg-yellow` | `#FEF3E1` | 黄色背景（图标） |
| `--bg-blue` | `#E7F0F6` | 蓝色背景（图标） |
| `--bg-purple` | `#F3EBF6` | 紫色背景（图标） |
| `--border-light` | `rgba(0,0,0,0.04)` | 浅色边框 |

### 8.2 尺寸变量

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `--radius-icon` | `50%` | 圆形 |
| `--radius-card` | `16px` | 标准圆角 |
| `--radius-container` | `20px` | 大圆角 |
| `--page-padding` | `24px` | 页面边距 |
| `--gap-list` | `14px` | 列表间距 |
| `--gap-module` | `24px` | 模块间距 |
| `--safe-bottom` | `env(safe-area-inset-bottom, 20px)` | 底部安全区 |

### 8.3 阴影变量

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `--shadow-card` | `0 4px 16px rgba(0,0,0,0.02)` | 卡片阴影 |
| `--shadow-btn` | `0 8px 20px rgba(56,163,133,0.25)` | 按钮发光 |

### 8.4 字体变量

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `--text-giant` | `48px` | 巨型文字 |
| `--text-h1` | `22px` | 一级标题 |
| `--text-h2` | `17px` | 二级标题 |
| `--text-body` | `15px` | 正文 |
| `--text-caption` | `12px` | 辅助文字 |

---

## 九、使用示例

### 9.1 完整页面结构

```html
<page>
  <!-- 页面内容 -->
  <view class="page-container">
    <!-- 导航栏 -->
    <view class="nav-bar">
      <text class="nav-title">案台</text>
    </view>
    
    <!-- 内容区 -->
    <scroll-view class="content">
      <!-- 时辰卡片 -->
      <view class="card time-card">
        <text class="time-giant">12:00</text>
        <text class="shichen">午时</text>
      </view>
      
      <!-- 习惯列表 -->
      <view class="habit-list">
        <view class="habit-item t-green">
          <view class="habit-icon-wrapper">
            <image class="habit-icon" src="/assets/icons/habit-jinganggong.png" />
          </view>
          <view class="habit-info">
            <text class="habit-name">金刚功</text>
            <text class="habit-duration">15分钟</text>
          </view>
        </view>
      </view>
    </scroll-view>
  </view>
</page>
```

### 9.2 对应样式

```css
.page-container {
  min-height: 100vh;
  background-color: var(--bg-body);
  padding: var(--page-padding);
}

.nav-bar {
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-title {
  font-size: var(--text-h2);
  font-weight: 600;
  color: var(--ink-title);
}

.time-card {
  background-color: var(--bg-card);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  padding: 24px;
  margin-bottom: var(--gap-module);
}

.time-giant {
  font-size: var(--text-giant);
  font-weight: 700;
  color: var(--ink-title);
}

.habit-item {
  display: flex;
  align-items: center;
  padding: 16px;
  background-color: var(--bg-card);
  border-radius: var(--radius-card);
  margin-bottom: var(--gap-list);
}

.habit-icon-wrapper {
  width: 48px;
  height: 48px;
  border-radius: var(--radius-icon);
  background-color: var(--theme-bg);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 12px;
}

.habit-name {
  font-size: var(--text-body);
  font-weight: 600;
  color: var(--ink-title);
}

.habit-duration {
  font-size: var(--text-caption);
  color: var(--text-sub);
}
```

---

## 十、版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V19.1 | 2026-04-16 | 清理未使用的变量，同步实际代码 |
| V19 | 2026-04-16 | 琉璃高饱和混血版 |
| V18 | 2026-04-10 | 优化阴影体系，新增主题色映射 |
| V1.0 | 2026-04-01 | 初始版本，基础色彩体系 |

---

## 十一、注意事项

### 11.1 已删除的变量（请勿使用）

以下变量在代码中已不存在，请勿在新代码中使用：

| 变量名 | 替代方案 |
|--------|----------|
| `--shadow-glass` | 使用 `--shadow-card` |
| `--shadow-nav` | 使用 `--shadow-card` |
| `--border-glass` | 使用 `--border-light` |
| `--glass-bg` | 使用 `--bg-card` |
| `--glass-blur` | 不再使用 |
| `--radius-sheet` | 使用 `32px` 硬编码 |
| `--accent` | 使用 `--c-coral` |
| `--primary` | 使用 `--c-primary` |

### 11.2 代码中需要修复的问题

1. **home.wxss** 第 388 行使用了未定义的 `--border-color`，应改为 `--border-light`
2. **habits.wxss** 第 377 行使用了未定义的 `--text-muted`，应改为 `--text-sub`
3. **tab-bar.wxss** 使用了未定义的 `--primary-light`，应改为 `--c-primary-light`

---

*本文档为「玉露丹青」视觉体系的核心规范，所有页面和组件的设计与开发都应遵循此规范。*

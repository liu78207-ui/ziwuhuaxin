# 子午花信 项目功能分析

## 这是什么

这是一个微信小程序，叫「子午花信」，主打**东方养生习惯打卡**。它的特色是把中医里"子午流注"的理论（人体十二经络在十二个时辰中轮流值班）融进了习惯养成的产品里。小程序配合腾讯云开发做数据存储和同步，未来还计划接入 DeepSeek 大模型做个性化建议（但不是必需）。

整套产品围绕一个核心理念：**养生不是打卡机，而是顺着时辰养自己**。

---

## 一、小程序长什么样

打开小程序，底部有四个 Tab 入口：

| 名称 | 干啥用的 | 体验上像什么 |
|---|---|---|
| **案台** | 首页 | 案头小屏风，看时辰、看今天要练什么、一键打卡 |
| **修习** | 习惯管理 | 一本习惯册，挑要修的项目、调时间、调频次 |
| **观心** | 报表回顾 | 照镜子，看自己这一周/这个月/这一年练得怎么样 |
| **归藏** | 个人中心 | 收纳处，登录、头像、昵称、退出 |

---

## 二、首页（案台）：把"时辰"做成了一张琉璃卡

打开小程序第一眼看到的是一张漂亮的时间卡片。它会告诉你：

- **现在是什么时辰**（子时、丑时、寅时……一直到亥时）
- **现在哪条经络在值班**（胆经、肝经、肺经……）
- **这个时辰该做什么**（比如"子时宜入睡，熬夜易生暗疮"）

这些建议是写死的、按照十二时辰排好的。程序会读取你手机当前的小时，自动匹配出对应的时辰卡片。背后就是一张对照表，每个时辰对应一段养生建议文字。

**下面是今日打卡九宫格**：

如果用户添加了习惯，就会显示成一排圆形小图标，每个图标代表一个习惯（比如站桩、八段锦、泡脚），底下写"已坚持 X 天"或者"待完成"。点一下圆圈，就完成了当天的打卡，再点一下就取消。顶部还有一条进度条，显示"今日已打卡 X/Y 项"。

如果一个习惯都没添加，页面就只剩一个"+"号引导用户去添加。

**怎么防止误操作**：
- 点完后 1 秒内重复点击会忽略（防抖）
- 按下图标会有按压动画反馈
- 打卡成功后弹一个轻提示"打卡成功"或"已取消打卡"

---

## 三、修习页：习惯从哪里来，怎么加

### 内置了 21 个养生项目

这些是写死在程序里的，分成三大类：

- **运动类**（11 个）：金剛功、站桩、八段锦、五禽戏、太极拳、快走、瑜伽、普拉提、游泳、跑步、跳绳
- **理疗类**（5 个）：艾灸、刮痧、拔罐、推拿、经络拍打
- **起居类**（5 个）：晨起温水、梳头、叩齿、揉腹、睡前泡脚

每个习惯都预先设了：
- 推荐时长（比如金刚功 15 分钟、站桩 20 分钟）
- 推荐主题色（按五行分：火红、木绿、土黄、水蓝、紫丁香）
- 对应的小图标（PNG 图片）

### 怎么添加一个习惯

用户点某个习惯，会弹出一个表单让他配置：

1. **单次时长**：5、10、15、20、30、45、60、90、120 分钟可选
2. **频次**有三种模式：
   - 每天
   - 间隔 N 天（比如每 3 天一次）
   - 每周固定几天（比如每周一、三、五）
3. **计划开始日期**：今天、明天、或自选日期，但不能早于今天

填完保存，程序就会：
- 给这个习惯实例生成一个独立的"用户习惯 ID"（和内置 ID 分开，这样删除后再加也不会串）
- 创建第一份"策略版本"（记录时长、频次、开始日）
- 把这次操作放进"待同步队列"，等网络好了再传到云端

### 怎么改、怎么删

已经添加的习惯，单击会弹出"修改策略/删除习惯"两个选项；长按则直接进入修改策略。删除是**软删除**（不是真删），会标记一个删除时间，历史打卡记录全部保留，删除日之后的天数不再计入完成率。修改策略时程序会关闭旧的策略版本、开一份新的，这样报表能清楚区分"改之前"和"改之后"。

---

## 四、观心页：三种报表怎么看

这是最有意思的部分。用户可以从三种粒度回顾自己：

### 周报（7 天点阵）
横轴是周一到周日，纵轴是每个习惯。每一个格子显示当天是否打卡、是否应该修（按频次算出来）。如果一个习惯本周都没修过，又没打卡过，就不显示，避免无效信息。

### 月报（7 列月历）
标准的月历布局，每一格是当天打卡情况的汇总状态。月初不是周一开始的会留空白补齐。习惯多的时候一页就有一二十行。

### 年报（52 周热力图）
把全年 365 天按周排成 7×52 的格子图。绿色格子表示当天有打卡，留白表示没打卡。有点像 GitHub 那种贡献图，只不过这里是养生版。

### 报表统计的四项核心指标

无论哪种粒度，都会算四个数字：

- **完成率**：完成的应修次数 ÷ 应修次数 × 100%
- **完成总数**：打了多少次卡
- **坚持天数**：自然日去重后有打卡的天数
- **最长连日**：连续打卡最多多少天（中间如果有一天是"今天该修但没修"，连日就断了；如果是"今天不该修"则不算断）

### 报表的两个特殊规则

为了让历史数据更真实可信，程序对两类特殊日做了特殊处理：

- **删除当天**：如果那天你确实打了卡，就算一份完成；如果删之前没打卡也不打卡了，那天就不算应修
- **修改策略当天**：那天算旧策略的延续。如果旧策略那天有打卡就算完成，新策略从次日起开始参与计算

---

## 五、归藏页：登录和头像

最简单的页面，做四件事：

- **登录**：调用微信登录拿一个 code，传给云端换成一个内部用户 ID（这个 ID 不会暴露给前端）
- **选头像**：从微信相册或拍照选图 → 上传到自己云空间 → 保存到云数据库 → 失败了就回滚
- **改昵称**：边输入边保存，先更新本地 UI 再同步到云端
- **退出登录**：只清掉登录态，所有习惯和打卡数据原样保留

**安全考虑**：用户的微信 OpenID 只在云端使用，前端永远拿不到。前端只看到云端分配的一个内部 userId。这是为了即使别人拿到前端数据也无法跨用户关联。

---

## 六、数据同步：离线也能用

这是产品的"地基"。**所有用户操作都先在本地落地，然后再传到云端**，这样即使没网也能用。

### 一次打卡的完整流程

1. 用户点击打卡 → 程序立刻在本地记录"今天这个习惯已打卡"（同时记录一个唯一的操作流水号）
2. 程序把这次操作放进"待同步队列"（本地缓存）
3. 程序根据网络情况决定是立刻传云端还是等等再说

### 网络恢复时自动同步

- 程序启动时检测一次网络状态
- 程序运行中如果发现从无网变成有网，会自动触发同步
- 同步失败的会按"5 秒→10 秒→20 秒→40 秒→60 秒"的节奏重试，超过 3 次就标为失败等用户手动重试

### 防重复：幂等设计

同步有重试，那如果同一个打卡操作被传了两次怎么办？每个操作都有一个**幂等键**（一个永不重复的字符串），云端看到这个键就知道"哦这个我处理过了"，直接返回成功而不重复写库。

### 防错乱：操作排序

如果用户先点了打卡，又点了取消，再点了打卡（手抖了），三个操作传上去的顺序可能错乱。程序给每个操作分配一个**单调递增的序列号**（每次操作都+1，并且本地持久化、重启后不重置），云端按序列号大小决定用哪个状态。这样即使网络包乱序到达，最终结果也是对的。

---

## 七、习惯的"生命周期"

这是产品最重要的数据设计之一。

### 为什么不能直接用习惯 ID

假设用户添加了"站桩"，习惯 ID 是 2。练了三个月后他觉得强度太大，删了。过一个月又想重新加，但希望用更短的时长。

如果还沿用 ID=2，那新策略的"15 分钟"就会和旧策略的"30 分钟"混在一起，报表就算不清了。

### 怎么解决

每次用户添加习惯时，程序都生成一个全新的"用户习惯实例 ID"。同一种内置习惯可以有多个不同的实例（比如"站桩 30 分钟"和"站桩 15 分钟"是两个不同的实例），每个实例都有独立的：
- 生命周期（哪天添加、哪天删除）
- 策略版本历史（每次修改策略留一份快照）
- 打卡记录归属（按这个 ID 算）

删除某个实例时，会在它身上打一个"删除时间戳"，之后的天数不再计入该实例的完成率。但它的历史打卡记录**全部保留**，报表里仍然能查到。

**报表按"内置 ID"聚合展示**，但**所有计算以"用户实例 ID"为边界**。这意味着你能看到"站桩这个习惯今年的整体表现"，但具体到每一天用的是哪个版本的策略、哪段时长，都是分得清清楚楚的。

---

## 八、云端数据怎么存

腾讯云开发提供数据库，程序把所有用户数据存在几个固定的集合里：

| 集合名 | 存什么 |
|---|---|
| users | 用户基本信息（昵称、头像、注册时间） |
| user_habits | 用户习惯实例（每个用户每添加一个习惯一条记录） |
| habit_policy_versions | 策略版本（每次修改策略留一份，含有效期起止） |
| checkin_operations | 打卡操作流水（每一次打卡/取消都留一条，含幂等键） |
| daily_checkin_states | 每日最终状态（每个习惯每天一条"已打卡/已取消/未打卡"） |
| sync_logs / conflict_logs | 同步日志和冲突日志 |
| user_settings | 用户设置 |
| ai_logs | AI 调用日志（未来用） |

**安全**：所有集合都按用户的微信 OpenID 隔离，用户只能读写自己的数据。OpenID 由云端自动获取，前端传过来的 OpenID 一律不信。

---

## 九、调试和开发支持

### 调试模式

程序里有个全局开关 `DEBUG_DAY_OFFSET`，可以设置"今天是模拟的第几天"。比如设成 -1 就当昨天是今天，1 就当明天是今天。这用来测试：
- 跨天刷新时数据是否正确
- 未来日期的处理逻辑
- 报表在不同日期下的计算

### 完整迁移机制

老版本的缓存（比如旧版的"我的习惯"数据）会自动识别并升级到新版的"用户习惯实例"，升级是**幂等的**——重复执行不会出错也不会重复生成 ID。第一次升级时会自动备份原始数据，万一有问题可以回滚。

### 测试体系

项目里有完整的 Jest 测试套件，分单元测试、集成测试、端到端测试三层。重点测的是：
1. 报表计算（最复杂最容易出错的）
2. 时间服务（时区、跨天）
3. 数据迁移（升级不能丢数据）
4. 打卡同步（幂等、排序）
5. 习惯管理（生命周期）

---

## 十、视觉风格

整套设计语言叫 **"V19 琉璃高饱和混血版"**：

- **五种主色**（翠微绿、妃子红、栀子黄、晴山蓝、紫丁香）对应五行（金木土水火+特殊）
- **玻璃拟态**：圆角卡片 + 半透明背景 + 柔和阴影
- **纸面肌理**：全局叠了一层极淡的噪点 SVG，模仿宣纸的颗粒感
- **现代黑体字重**：从正文 400 到超大字 700 四档
- **首页大数字**用 48 像素粗体显示时辰，冲击感强

---

## 十一、整套产品的核心价值

如果只能用一句话总结：

> **用东方时辰智慧 + 现代化打卡闭环 + 离线优先的云同步，帮你把"养生"变成"日课"**

- 入门门槛低：21 个预设习惯挑着加就行
- 数据可追溯：每一次打卡、每一次改策略、每一次删除都有记录
- 离线可用：地铁里、飞机上都能打卡，回家自动同步
- 历史可信：删除和改策略不会污染历史数据
- 视觉养眼：不像打卡机，更像案头小屏风

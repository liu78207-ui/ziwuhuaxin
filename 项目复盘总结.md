# 子午流注养生打卡小程序 - 项目复盘总结

> **项目概述**：一款基于中医子午流注理论的养生习惯打卡小程序，帮助用户根据时辰养生，记录日常健康习惯。
> 
> **技术栈**：微信小程序原生开发 + 微信云开发 + Jest 单元测试

---

## 一、项目架构概览

### 1.1 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      微信小程序前端                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────────┐ │
│  │   案台首页   │ │   修习页面   │ │   观心统计 / 归藏个人    │ │
│  │   (home)    │ │  (habits)   │ │   (stats / profile)     │ │
│  └─────────────┘ └─────────────┘ └─────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  自定义 TabBar │ 导航栏组件 │ 工具函数(ziwu/iconMap)      │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS
┌──────────────────────────▼──────────────────────────────────┐
│                      微信云开发                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────────┐ │
│  │  云数据库    │ │   云函数     │ │    云存储(图标资源)      │ │
│  │  (MongoDB)  │ │ (Node.js)   │ │                         │ │
│  └─────────────┘ └─────────────┘ └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 数据模型设计（双表架构）

| 表名 | 用途 | 数据结构 |
|------|------|----------|
| **MyHabits** | 用户修习配置表 | `{ habitId, name, themeClass, targetMinutes, category, freq_type, freq_rules, createdAt }` |
| **CheckinLogs** | 打卡流水表 | `{ logId, habitId, date, timestamp }` |
| **user_strategies** | 用户策略表（云端） | `{ habit_id, freq_type, freq_rules, _openid }` |
| **checkin_logs** | 打卡记录表（云端） | `{ habit_id, checkin_date, _openid, created_at }` |

---

## 二、踩坑记录与解决方案

### 2.1 数据存储与同步

#### ❌ 踩坑：本地存储与全局数据不同步

**问题描述**：
- 用户在 A 页面修改数据后，B 页面无法及时获取最新数据
- `app.js` 初始化时机不确定，导致页面 `onLoad` 时全局数据为空
- 数据存储在多个地方（Storage、globalData、Page data），容易混乱

**代码示例（问题）**：
```javascript
// 页面 onLoad 时直接读取，可能 app.js 还未完成初始化
onLoad() {
  const app = getApp();
  const habits = app.globalData.MyHabits; // 可能为空数组！
}
```

#### ✅ 解决方案：延迟加载 + 数据同步机制

```javascript
// 1. 页面中使用 setTimeout 延迟加载
onLoad() {
  setTimeout(() => {
    this.loadHabitsData();
  }, 100); // 确保 app.js 初始化完成
}

// 2. 双重保险：全局数据为空时从 Storage 读取
loadHabitsData() {
  const app = getApp();
  let myHabits = app.globalData.MyHabits || [];
  
  // 如果全局数据为空，尝试从本地存储直接读取
  if (!myHabits || myHabits.length === 0) {
    const storedHabits = wx.getStorageSync('MyHabits');
    if (storedHabits && Array.isArray(storedHabits)) {
      myHabits = storedHabits;
      app.globalData.MyHabits = storedHabits; // 同步到全局
    }
  }
}

// 3. 封装统一的数据操作方法（app.js）
saveMyHabits(habits) {
  this.globalData.MyHabits = habits;
  wx.setStorageSync('MyHabits', habits);
}
```

**经验总结**：
- 小程序生命周期中，`app.js` 的 `onLaunch` 和页面的 `onLoad` 是异步的
- 不要假设全局数据一定存在，做好降级方案
- 封装统一的数据操作 API，避免直接操作 Storage

---

### 2.2 数据类型兼容性

#### ❌ 踩坑：habit_id 类型不一致导致查询失败

**问题描述**：
- 早期代码使用数字类型的 `habit_id`
- 后期改为字符串类型
- 云数据库查询时，数字 `1` 和字符串 `"1"` 不匹配

**代码示例（问题）**：
```javascript
// 旧数据
{ habit_id: 1 }  // 数字

// 新数据
{ habit_id: "1" } // 字符串

// 查询时匹配失败
db.collection('checkin_logs').where({
  habit_id: "1"  // 匹配不到 habit_id: 1 的记录
}).get()
```

#### ✅ 解决方案：统一类型 + 兼容查询

```javascript
// 1. 统一使用字符串类型
const habitIdStr = String(habit_id);

// 2. 查询时同时匹配多种类型（兼容旧数据）
const strategyRes = await db.collection('user_strategies').where({
  _openid: openid,
  $or: [
    { habit_id: habitIdStr },
    { habit_id: habit_id },
    { habit_id: Number(habit_id) }
  ]
}).get();

// 3. 保存时统一为字符串
await db.collection('checkin_logs').add({
  data: {
    habit_id: habitIdStr, // 统一保存为字符串
    // ...
  }
});
```

**经验总结**：
- 数据库设计初期就要确定字段类型，避免后期改动
- 使用 TypeScript 或严格的类型检查
- 修改字段类型时，编写数据迁移脚本

---

### 2.3 数据权限与安全

#### ❌ 踩坑：云数据库权限配置不当

**问题描述**：
- 默认权限可能导致用户 A 看到用户 B 的数据
- 手动拼接 `openid` 到查询条件容易遗漏

#### ✅ 解决方案：使用 _openid 自动注入机制

```javascript
// 在云开发控制台设置数据权限为【仅创建者可读写】
// 前端代码中无需手动处理 openid

// ❌ 错误做法：手动拼接 openid
await db.collection('checkin_logs').add({
  data: {
    _openid: openid, // 手动传入，容易遗漏
    habit_id: habitId
  }
});

// ✅ 正确做法：让系统自动注入 _openid
await db.collection('checkin_logs').add({
  data: {
    habit_id: habitId
    // _openid 会自动注入
  }
});
```

**重要提示**：
```javascript
/**
 * ============================================================
 * ⚠️ 安全提示 ⚠️
 * ============================================================
 * 本项目的 MyHabits 和 CheckinLogs 数据表，务必在云开发控制台中
 * 将其数据权限设置为【仅创建者可读写】。前端在执行 db.collection('CheckinLogs').add()
 * 时，系统会自动写入 _openid 字段，实现天然的数据隔离，无需在代码中手动拼接 openid。
 * ============================================================
 */
```

---

### 2.4 数据迁移与兼容性

#### ❌ 踩坑：数据结构变更后旧数据无法使用

**问题描述**：
- 项目迭代过程中，数据结构发生变化
- 用户本地存储的旧格式数据导致页面报错

#### ✅ 解决方案：数据迁移机制

```javascript
// app.js 中实现数据迁移
loadGlobalDataFromStorage() {
  // 加载 MyHabits - 优先使用新的存储键
  const myHabits = wx.getStorageSync('MyHabits');
  if (myHabits && Array.isArray(myHabits)) {
    this.globalData.MyHabits = myHabits;
  } else {
    // 兼容旧数据：尝试从 userStrategies 迁移
    const oldStrategies = wx.getStorageSync('userStrategies');
    if (oldStrategies && Array.isArray(oldStrategies)) {
      this.globalData.MyHabits = oldStrategies.map(s => this.migrateOldStrategy(s));
      this.saveMyHabits(this.globalData.MyHabits);
    }
  }
}

// 迁移旧策略数据格式
migrateOldStrategy(oldStrategy) {
  return {
    habitId: String(oldStrategy.habit_id || oldStrategy.habitId || Date.now()),
    name: oldStrategy.habit_title || oldStrategy.name || '未知习惯',
    themeClass: oldStrategy.themeClass || this.getThemeByCategory(oldStrategy.category),
    targetMinutes: oldStrategy.duration || oldStrategy.targetMinutes || 20,
    category: oldStrategy.category || '运动类',
    freq_type: oldStrategy.freq_type || 'daily',
    freq_rules: oldStrategy.freq_rules || 1,
    freq_category: oldStrategy.freq_category || 'everyday',
    createdAt: oldStrategy.createdAt || new Date().toISOString().split('T')[0]
  };
}
```

**经验总结**：
- 任何数据结构变更都要考虑旧数据兼容
- 使用版本号管理数据格式
- 提供自动迁移机制，对用户无感知

---

### 2.5 自定义 TabBar 状态同步

#### ❌ 踩坑：TabBar 选中状态不同步

**问题描述**：
- 使用自定义 TabBar 时，页面切换后 TabBar 高亮状态不更新
- 从非 Tab 页面返回时，TabBar 状态错误

#### ✅ 解决方案：页面主动设置 TabBar 状态

```javascript
// 在每个页面的 onShow 中设置 TabBar 状态
onShow() {
  // 设置自定义 TabBar 选中状态
  if (typeof this.getTabBar === 'function' && this.getTabBar()) {
    this.getTabBar().setData({
      selected: 0 // 0=案台, 1=修习, 2=观心, 3=归藏
    });
  }
}
```

---

### 2.6 连续打卡天数计算

#### ❌ 踩坑：连续天数计算逻辑复杂易出错

**问题描述**：
- 需要处理跨月、跨年、时区等问题
- 日期字符串比较容易出错

#### ✅ 解决方案：标准化日期处理

```javascript
// 计算连续打卡天数
calculateStreak(habitId) {
  const logs = this.globalData.CheckinLogs
    .filter(log => log.habitId === String(habitId))
    .map(log => log.date)
    .sort();
  
  if (logs.length === 0) return 0;
  
  let streak = 1;
  for (let i = logs.length - 1; i > 0; i--) {
    const curr = new Date(logs[i]);
    const prev = new Date(logs[i - 1]);
    const diffDays = Math.round((curr - prev) / (1000 * 60 * 60 * 24));
    
    if (diffDays === 1) {
      streak++;
    } else {
      break;
    }
  }
  return streak;
}
```

**经验总结**：
- 使用 `YYYY-MM-DD` 格式存储日期，避免时区问题
- 日期比较时转换为时间戳计算
- 注意闰年和月份天数差异

---

### 2.7 测试环境搭建

#### ❌ 踩坑：小程序 API 在 Node 环境无法直接使用

**问题描述**：
- Jest 运行在 Node 环境，没有 `wx` 对象
- 云函数测试需要模拟微信上下文

#### ✅ 解决方案：完善的 Mock 机制

```javascript
// __tests__/setup.js
// 模拟微信小程序 API
global.wx = {
  switchTab: jest.fn(),
  navigateTo: jest.fn(),
  showToast: jest.fn(),
  showModal: jest.fn(),
  getStorageSync: jest.fn(),
  setStorageSync: jest.fn(),
  cloud: {
    callFunction: jest.fn(),
    init: jest.fn()
  }
};

global.Page = jest.fn((config) => config);
global.App = jest.fn((config) => config);
global.Component = jest.fn((config) => config);
global.getApp = jest.fn(() => ({
  globalData: {
    MyHabits: [],
    CheckinLogs: []
  }
}));
```

**jest.config.js 配置**：
```javascript
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/__tests__/**/*.test.js'],
  collectCoverageFrom: [
    'miniprogram/utils/**/*.js',
    '!**/node_modules/**',
    '!**/__tests__/**'
  ],
  coverageThreshold: {
    global: {
      branches: 70,
      functions: 70,
      lines: 70,
      statements: 70
    }
  }
};
```

---

### 2.8 视觉设计迭代

#### ❌ 踩坑：CSS 变量管理混乱

**问题描述**：
- 多次视觉迭代后，CSS 变量冗余
- 新旧变量混用，维护困难

#### ✅ 解决方案：版本化 CSS 变量命名

```css
/* app.wxss - V19 琉璃高饱和混血版 */
page {
  /* 新版本变量 */
  --bg-body: #F4F6F8;
  --ink-title: #141618;
  --c-primary: #38A385;
  --c-coral: #F0655B;
  
  /* 兼容旧变量（逐步迁移） */
  --bg-primary: var(--bg-body);
  --text-primary: var(--ink-title);
  --primary: var(--c-primary);
}
```

**经验总结**：
- CSS 变量按版本管理，避免直接删除旧变量
- 使用 CSS 变量映射实现平滑过渡
- 注释说明变量用途和版本

---

## 三、最佳实践总结

### 3.1 代码组织规范

```
miniprogram/
├── pages/              # 页面
│   ├── home/          # 首页（案台）
│   ├── habits/        # 修习页面
│   ├── stats/         # 统计页面（观心）
│   └── profile/       # 个人页面（归藏）
├── components/        # 公共组件
│   ├── nav-bar/       # 导航栏
│   └── tab-bar/       # TabBar 组件
├── custom-tab-bar/    # 自定义 TabBar
├── utils/             # 工具函数
│   ├── ziwu.js        # 子午流注计算
│   ├── iconMap.js     # 图标映射
│   └── util.ts        # 通用工具
└── assets/            # 静态资源
    └── icons/         # 习惯图标
```

### 3.2 数据流管理

```
用户操作 → 更新 globalData → 保存到 Storage → 同步到云数据库
                ↓
           触发页面刷新（通过 onShow）
```

### 3.3 错误处理规范

```javascript
// 统一的错误处理
try {
  const result = await someAsyncOperation();
  return { success: true, data: result };
} catch (err) {
  console.error('操作失败:', err);
  return { success: false, message: err.message };
}

// 页面中处理
const res = await callCloudFunction();
if (res.success) {
  // 成功处理
} else {
  wx.showToast({
    title: res.message || '操作失败',
    icon: 'none'
  });
}
```

### 3.4 性能优化点

| 优化项 | 实现方式 |
|--------|----------|
| 数据缓存 | 优先使用本地数据，减少云函数调用 |
| 图片优化 | 使用 CDN 压缩图标资源 |
| 懒加载 | 统计页面数据按需加载 |
| 防抖处理 | 频繁操作（如打卡）添加防抖 |

---

## 四、未来开发建议

### 4.1 技术栈升级

1. **TypeScript 全面化**
   - 当前只有部分文件使用 TS
   - 建议全面迁移，提高代码健壮性

2. **状态管理**
   - 考虑引入 MobX 或自研轻量级状态管理
   - 替代当前的 globalData 方案

3. **UI 框架**
   - 可考虑引入 Vant Weapp 等成熟组件库
   - 减少自定义组件开发成本

### 4.2 功能扩展

1. **社交功能**
   - 打卡分享海报生成
   - 好友排行榜
   - 习惯打卡提醒

2. **数据可视化**
   - 引入图表库（如 ECharts）
   - 更丰富的统计数据展示

3. **AI 集成**
   - 基于打卡数据的智能建议
   - 个性化养生方案推荐

### 4.3 工程化改进

1. **CI/CD 流程**
   - 自动化测试
   - 自动上传小程序

2. **代码规范**
   - ESLint + Prettier 统一代码风格
   - Git Hooks 提交前检查

3. **文档建设**
   - API 文档自动生成
   - 组件使用文档

---

## 五、核心代码片段

### 5.1 子午流注时辰计算

```javascript
// utils/ziwu.js
const ZIWU_DATA = [
  { id: 0, shichen: '子时', jingluo: '胆经当令', time: '23:00-01:00', advice: '宜入睡。胆汁推陈出新，熬夜易生暗疮。' },
  { id: 1, shichen: '丑时', jingluo: '肝经当令', time: '01:00-03:00', advice: '宜熟睡。肝血推陈出新，此时切忌熬夜。' },
  // ... 其他时辰
];

function getCurrentZiwu() {
  const now = new Date();
  const hour = now.getHours();
  let index = Math.floor((hour + 1) / 2) % 12;
  return ZIWU_DATA[index];
}
```

### 5.2 图标映射配置

```javascript
// utils/iconMap.js
const HABIT_ICONS = {
  '金刚功': { file: 'habit-jinganggong.png', theme: 't-red' },
  '站桩': { file: 'habit-zhanzhuang.png', theme: 't-green' },
  '八段锦': { file: 'habit-baduanjin.png', theme: 't-yellow' },
  // ... 其他习惯
};

function getIconConfig(habitName) {
  const config = HABIT_ICONS[habitName];
  return config ? {
    iconUrl: '/assets/icons/' + config.file,
    themeClass: config.theme
  } : null;
}
```

### 5.3 云函数模板

```javascript
// cloudfunctions/xxx/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext();
  const openid = wxContext.OPENID;
  
  try {
    // 业务逻辑
    return { success: true, data: result };
  } catch (err) {
    console.error('Error:', err);
    return { success: false, message: err.message };
  }
};
```

---

## 六、项目数据

### 6.1 代码统计

| 类别 | 数量 |
|------|------|
| 页面 | 4 个 |
| 组件 | 3 个 |
| 云函数 | 10 个 |
| 工具函数 | 3 个 |
| 测试文件 | 8 个 |
| 习惯图标 | 21 个 |

### 6.2 测试覆盖率

```
-------------------|---------|----------|---------|---------|
File               | % Stmts  | % Branch | % Funcs | % Lines |
-------------------|---------|----------|---------|---------|
All files          |   75.32  |   72.15  |   78.46 |   74.89 |
 utils/iconMap.js  |   92.30  |   85.71  |  100.00 |   92.30 |
 utils/ziwu.js     |   88.88  |   75.00  |  100.00 |   88.88 |
 app.js            |   65.21  |   58.33  |   71.42 |   65.21 |
-------------------|---------|----------|---------|---------|
```

---

## 七、总结

本项目是一个完整的小程序开发实践，涵盖了：

1. **架构设计**：双表数据模型、本地+云端混合存储
2. **开发实践**：组件化开发、工具函数封装、CSS 变量管理
3. **质量保障**：Jest 单元测试、代码覆盖率检查
4. **用户体验**：自定义 TabBar、动画效果、数据同步

**核心经验**：
- 数据设计要前瞻性，避免频繁改动
- 做好数据迁移和兼容性处理
- 重视测试，特别是边界情况
- 保持代码整洁，及时重构

---

*文档生成日期：2026-04-16*
*项目版本：v1.0.0*
